# open-science-project plugin skills

One directory per skill: new-project, new-task, context-files, migrate-project,
update-from-template, private-investigation. Scripts the hooks run are in `../scripts/`; `tests/test_skills.py`
checks that every command and script a skill names exists.
