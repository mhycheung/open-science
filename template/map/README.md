# The logic of the project

<!-- Hand-written by the main agent and the user, at most 150 lines: what the project
tries to establish, the main line of argument, and how the tasks fit into it. Link to the
task maps (tasks/<id>/map.md) for what is inside a task. -->

Generated from the node headers by `opsci map build` (never edit these by hand):

- [`graph.md`](graph.md): every node and its edges.
- [`dead_ends.md`](dead_ends.md): every failed, superseded or abandoned node, with the reason.
- [`claims.md`](claims.md): the claims graph. Every result, grouped by the task it came from,
  what it rests on (results, datasets, external works), what uses it, where it is stored,
  and the results that rest on failed or superseded work.
